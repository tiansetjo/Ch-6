<?php
session_start();
// membuat koneksi ke database

$conn = mysqli_connect("localhost", "root","", "game");


//Menambah user baru
if (isset($_POST["addnewplayer"])){
    $username = $_POST ["Nama User"];
    $alamat = $_POST ["alamat"];
    $umur = $_POST ["umur"];

    $addtotable = mysqli_query($conn, "insert into stock(Nama user, alamat, umur) VALUES ($username, $alamat,$umur)");

    if ($addtotable){
        header('location:index.php');
    } else {
        echo "gagal";
    };

};
?>